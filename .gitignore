# Node
node_modules/
/dist/
/coverage/
*.tsbuildinfo
*.log
npm-debug.log*

# lib/ is committed on purpose: the release pipeline publishes from the
# committed build output and CI asserts it matches a fresh build
# (git diff --exit-code -- lib).

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# agent memory artifacts (Claude Code, Codex, ...)
.remember/
