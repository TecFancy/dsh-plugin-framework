#!/usr/bin/env sh
# Pre-push: scenario gate run. `run-gates.mjs` auto-detects the change
# surface against the upstream ref and runs the minimal evidence set — the
# full suite stays CI's job, this stays fast enough to actually run every
# push. Falls back to HEAD when there is no upstream yet.
npm run gates -- --base @{u}