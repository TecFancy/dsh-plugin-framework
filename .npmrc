# The framework and its plugins only rely on public packages
# (@deepseek-ai/cordis, @deepseek-ai/dsh-tools, ...), so pin the official
# registry for this repo regardless of any machine-level .npmrc override.
registry=https://registry.npmjs.org/