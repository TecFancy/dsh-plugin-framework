node_modules/
/lib/
/coverage/
/dist/
package-lock.json

# release-please generated changelog — its `*` bullets differ from Prettier's
# markdown style; formatting it here would make the release PR fail format:check
CHANGELOG.md

# husky hook scripts have no extension - prettier cannot infer a parser
.husky/
